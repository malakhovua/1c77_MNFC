Процедура ПриЗаписи()
	Если глКодКонтрагентаУникален(ТекущийЭлемент(), Контрагент, Номенклатура, КодКонтрагента, Выбран()) = Ложь Тогда 
		Предупреждение("У данного элемента справочника может быть только одно значение ""Код контрагента"". Запись невозможна");
		СтатусВозврата(0);
	КонецЕсли;
КонецПроцедуры