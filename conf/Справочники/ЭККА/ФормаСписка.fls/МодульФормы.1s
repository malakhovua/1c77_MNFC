// ===============================
Процедура ИзмМагазин()
	Если Магазин.ВидСклада <> Перечисление.ВидыСкладов.Розничный Тогда
		Предупреждение("Можно выбирать только розничные склады");
	    Магазин = 0;
	КонецЕсли;
КонецПроцедуры

// ======================================
Процедура ПриОткрытии()
	Форма.кПравоваяПоддержка.Видимость(глВидимостьПравовойПоддержки);
КонецПроцедуры

