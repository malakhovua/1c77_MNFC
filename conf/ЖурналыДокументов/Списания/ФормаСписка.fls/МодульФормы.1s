// ======================================
Процедура ПриОткрытии()
	Форма.кПравоваяПоддержка.Видимость(глВидимостьПравовойПоддержки);
КонецПроцедуры 

Функция Пром()
	
	Если ПустоеЗначение(ТекущийДокумент) <> 1 Тогда
		Возврат ТекущийДокумент.Пром
	КонецЕсли;	
	
КонецФункции

Функция ПромФарша()
	
	Если ПустоеЗначение(ТекущийДокумент) <> 1 Тогда
		Возврат ТекущийДокумент.ПромФарша
	КонецЕсли;	
	
КонецФункции
